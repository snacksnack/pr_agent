# PR Review Agent — runtime dependencies
# RC1-107 scaffold. Packages used by later stories are noted inline so the
# skeleton stays installable as we build out each milestone.

# Config (RC1-107)
pydantic>=2.7
pydantic-settings>=2.3

# Agent loop + reviews (RC1-108 .. RC1-114)
anthropic>=0.40
httpx>=0.27

# Webhook service (RC1-116)
fastapi>=0.111
uvicorn[standard]>=0.30

# GitHub App auth (RC1-115)
PyJWT>=2.8
cryptography>=42

# Dev / tests
pytest>=8
# RC1-253: the shared eval harness. Pinned by tag — if the ruler can move under
# a run, a changed score no longer says whether the reviewer drifted or the
# measurement did.
agent-evals[sql] @ git+https://github.com/snacksnack/agent-evals@v0.2.0
pytest-cov>=5
ruff>=0.7
